# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Alert-SwiftUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Alert-SwiftUI

  target 'Alert-SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Alert-SwiftUIUITests' do
    # Pods for testing
  end

end
